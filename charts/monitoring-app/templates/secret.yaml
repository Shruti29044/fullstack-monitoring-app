apiVersion: v1
kind: Secret
metadata:
  name: {{ include "monitoring-app.fullname" . }}-secrets
type: Opaque
data:
  # Base64-encoded Yelp API key. Override with --set-file or valuesSecret.
  yelp-api-key: ""

