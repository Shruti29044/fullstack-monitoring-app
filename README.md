Fullstack Monitoring App

Quick start:
- Backend: `npm run dev` (set `YELP_API_KEY` first)
- Frontend: `cd web && npm run dev`

CI:
- GitHub Actions builds and pushes Docker images on every push to `main`.

